kirei-filter
PWAのインストール方法
iPhone / iPad（Safari）
`kirei-filter` のURLを Safari で開きます。
画面下の 共有ボタン を押します。
「ホーム画面に追加」 を選びます。
名前を確認して 追加 を押します。
ホーム画面に追加されたアイコンから起動してください。
Android（Chrome）
`kirei-filter` のURLを Chrome で開きます。
右上メニューから 「ホーム画面に追加」 または 「アプリをインストール」 を選びます。
確認画面で 追加 / インストール を押します。
追加後はホーム画面のアイコンから起動してください。
うまく更新が反映されない場合
PWA は古いキャッシュが残ることがあります。表示が変わらない場合は、次を試してください。
ページを閉じて開き直す
ブラウザで再読み込みする
シークレットモードで開く
ホーム画面版を削除して入れ直す
---
アプリ概要
`kirei-filter` は、画像の内容を AI が見て、全体の「整え」方向を提案する試作アプリです。
対象の例:
顔写真
食事写真
UI / デザイン画像
プロダクト写真
室内や一般的な写真
この試作では、画像そのものを大きく作り変えるのではなく、主役を壊さず、ノイズを減らす方向 を重視しています。
---
認証について
このアプリは、最初の一回だけ認証キーワード（PW）を入力して使う構成です。  
一度認証が通ると、その端末では次回以降自動で利用できます。
認証キーワードは公開していません。  
必要な方には個別にPWをお知らせします。
---
使い方
初回のみ認証キーワードを入力します。
画像を選択します。
AI が画像内容を判定します。
内容に応じて「整え」方向の補正提案を返します。
必要に応じて再度別画像で試します。
---
動作環境
Cloudflare Pages / Pages Functions
OpenAI API
モダンブラウザ（Safari / Chrome 推奨）
---
